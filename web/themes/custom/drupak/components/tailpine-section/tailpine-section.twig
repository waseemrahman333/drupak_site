      <!-- Tailpine -->
        <section class="bg-secondary container-px py-12.5 md:py-20 ">
            <div class="flex flex-col md:flex-row mx-auto max-w-480">
                <div
                    class="w-full flex flex-col gap-6 2xl:gap-8 bg-body-bg p-[clamp(20px,5vw,56px)] md:mr-[clamp(0px,9vw,200px)] min-h-145 md:min-h-0 mb-50 md:mb-0">
                    <div class="basis-1/3">
                        <h2
                            class="uppercase text-h2 relative pb-5 text-tertiary max-w-106 after:h-1 after:w-15 after:content-[] after:bg-tertiary after:absolute after:bottom-0 after:left-0">
                            {{title}}</h2>
                    </div>
                    <div class="xl:max-w-185 md:max-w-120 flex flex-col gap-8">
                        <div class="font-open-sans leading-7 lg:text-lg lg:leading-7.5 text-tertiary">{{description}}</div>
                     {{cta}}
                    </div>


                </div>
            
                <div class="relative">
                    <img src="{{tailpineimage}}" class="w-[clamp(268px,22vw,370px)] h-[clamp(340px,22vw,490px)] absolute bottom-0 left-1/2 -translate-x-1/2 md:bottom-auto md:left-auto md:top-1/2 md:right-0 md:translate-x-0 md:-translate-y-1/2" />
                    </img>

                </div>
            </div>
        </section>

  
       