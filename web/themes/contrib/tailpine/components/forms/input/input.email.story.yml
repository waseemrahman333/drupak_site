name: Email Input
description: Email input field with validation

slots:
  default: |
    <div class="p-6 bg-white">
      <div class="form-item mb-4">
        <label class="block text-sm font-semibold text-gray-900 mb-2">Email Input</label>
        <input type="email" name="user_email" placeholder="your.email@example.com" class="!bg-white border border-gray-300 text-gray-900 text-base focus:ring-2 focus:ring-primary focus:border-primary block w-full px-4 py-3 transition-all placeholder:text-gray-400 hover:border-gray-400" />
      </div>
    </div>
