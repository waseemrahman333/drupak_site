name: Form Element with Error
description: Form element showing validation error state

slots:
  label: "Password"
  children: |
    <input type="password" name="password" class="bg-white border-2 border-error text-gray-900 text-base focus:ring-2 focus:ring-error focus:border-error block w-full px-4 py-3 transition-all" />

props:
  type: password
  name: password
  title_display: before
  description_display: after
  disabled: false
  errors: true

