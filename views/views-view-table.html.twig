{#
/**
 * @file
 * Theme override for displaying a view as a table.
 *
 * Available variables:
 * - attributes: HTML attributes for the table element.
 * - title: The title of this group of rows.
 * - header: The table header columns.
 * - rows: Table row items.
 * - responsive: Flag indicating whether the table is responsive.
 * - sticky: Flag indicating whether the table header is sticky.
 *
 * @see template_preprocess_views_view_table()
 */
#}

{{ include('tailpine:viewtable') }}
